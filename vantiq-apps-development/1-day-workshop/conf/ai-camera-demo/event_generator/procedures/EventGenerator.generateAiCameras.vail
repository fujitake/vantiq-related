PROCEDURE EventGenerator.generateAiCameras(config Object)
var aiCameras = []
for i in range(1, config.num_of_ai_camera + 1, 1) {
    push(aiCameras, { 
        camera_id: "camera_00" + i
    })
}
return aiCameras
